export const FILTER_TYPES = {
	ALL: "all",
	ACTIVE: "active",
	COMPLETED: "completed",
} as const;

export const STORAGE_KEYS = {
	TODOS: "todo-app-todos",
	FILTER: "todo-app-filter",
} as const;
